import app, { auth, storage, googleProvider, registerWithEmail, loginWithEmail, loginWithGoogle, logoutUser } from '../services/firebase';

export {
  auth,
  storage,
  googleProvider,
  registerWithEmail,
  loginWithEmail,
  loginWithGoogle,
  logoutUser
};

export default app;
